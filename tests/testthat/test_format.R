context("Testing format_PDS function")

test_that("Cleaned data has correctly formatted D grades", {
})

test_that("Lifts column contains only numeric values")

test_that("Number of floors is a number, not a factor")

test_that("Swimming pool column entries are 1 or 0")

test_that("Formatted data contains everything needed for cleaning functions")

test_that("Formatted data contains all columns required by Blockbuster2")
